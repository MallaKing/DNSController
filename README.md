# DNSController
A local DNS firewall and sinkhole controller built in Python that runs as a custom DNS resolver on Windows.   It acts as a middle layer between client applications and upstream DNS servers and applies filtering, SafeSearch enforcement, caching, and domain blocking in real time.
